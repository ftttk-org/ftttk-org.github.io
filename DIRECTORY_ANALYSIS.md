# ディレクトリ構造分析レポート

## 📁 現在のディレクトリ構造

```
tkgithub/
├── assets/                      # アセット（SCSS、アイコン）
│   ├── icons/logo.png          # ロゴアイコン
│   └── scss/
│       └── _variables_project.scss  # Docsy用SCSS変数（不要）
├── content/                     # コンテンツ（標準）
│   ├── _index.md               # ホームページコンテンツ
│   ├── featured-background.jpg # ヒーロー背景画像
│   ├── join/                   # 訓練参加情報
│   ├── resources/              # リソース・メディア
│   ├── testimonies/            # 恵みの証し
│   └── training/               # 訓練について
├── layouts/                     # カスタムレイアウト（標準）
│   ├── index.html              # ホームページレイアウト（カスタマイズ）
│   ├── 404.html                # 404ページ（カスタマイズ）
│   ├── _default/               # 空（不要）
│   ├── _markup/
│   │   └── render-heading.html # 見出しレンダリング
│   ├── _partials/              # 空（不要）
│   ├── partials/               # 空（不要）
│   └── shortcodes/
│       ├── blocks/             # Docsy互換ショートコード
│       │   ├── cover.html
│       │   ├── lead.html
│       │   ├── link-down.html
│       │   └── section.html
│       ├── join-pages.html     # カスタムショートコード
│       ├── recentposts.html    # カスタムショートコード
│       └── training-pages.html # カスタムショートコード
├── static/                      # 静的ファイル（標準）
│   ├── favicons/               # ファビコン
│   └── images/
│       ├── featured-background.jpg  # ヒーロー画像
│       └── site-logo.png       # サイトロゴ
├── themes/                      # テーマ（標準）
│   ├── PaperMod/               # 現在のテーマ（Git submodule）
│   └── custom/                 # 以前のカスタムテーマ
│       ├── layouts/
│       │   ├── _default/
│       │   │   ├── baseof.html
│       │   │   ├── list.html
│       │   │   └── single.html
│       │   └── partials/
│       │       ├── header.html
│       │       └── footer.html
│       ├── static/css/style.css
│       └── theme.toml
├── public/                      # ビルド出力（Git除外対象）
├── hugo.yaml                    # Hugo設定（標準）
├── package.json                # npm設定（Docsy用）
├── postcss.config.js           # PostCSS設定（Docsy用）
├── docker-compose.yaml         # Docker設定
├── netlify.toml               # Netlify設定
├── LAYOUT_STRUCTURE.md        # レイアウト説明書
└── .gitmodules                # Git submodules設定
```

---

## 🔴 不要なファイル・ディレクトリ

### 1. **完全に不要（削除推奨）**

#### Docsy関連の残骸
```
assets/scss/_variables_project.scss    # Docsy専用SCSS
postcss.config.js                      # Docsy用PostCSS設定
```

#### 空のディレクトリ
```
layouts/_default/     # 空
layouts/_partials/    # 空
layouts/partials/     # 空
```

#### カスタムショートコード（使用されていない可能性）
```
layouts/shortcodes/join-pages.html       # joinページのカスタム表示
layouts/shortcodes/training-pages.html   # trainingページのカスタム表示
layouts/shortcodes/recentposts.html      # 最近の投稿表示
```

#### アセット
```
assets/icons/logo.png    # 使用されているか不明（static/images/site-logo.pngがある）
```

---

### 2. **条件付き不要（テーマ依存）**

#### customテーマ全体（現在theme: PaperModを使用）
```
themes/custom/                          # 全体が不要
├── layouts/_default/baseof.html        # PaperModが提供
├── layouts/_default/list.html          # PaperModが提供
├── layouts/_default/single.html        # PaperModが提供
├── layouts/partials/header.html        # PaperModが提供
├── layouts/partials/footer.html        # PaperModが提供
├── static/css/style.css                # PaperModが提供
└── theme.toml                          # 不要
```

**注意**: `theme: custom`に戻す場合は必要

---

### 3. **ビルド出力（Git管理から除外すべき）**

```
public/                                 # ビルド出力（.gitignoreに記載済み）
.hugo_build.lock                        # Hugoロックファイル
```

---

## ⚠️ Hugoデフォルトと異なる構造・実装

### 1. **カスタムホームページレイアウト**
```
layouts/index.html    # 完全カスタム（ftttk-org.github.io風）
```
**影響**: テーマを変更すると、このカスタムレイアウトが優先される  
**推奨**: テーマのホームページを使いたい場合は削除

---

### 2. **カスタム404ページ**
```
layouts/404.html      # カスタムテーマ風のスタイル
```
**影響**: テーマの404ページが使われない  
**推奨**: テーマのデフォルトを使う場合は削除

---

### 3. **Docsy互換ショートコード**
```
layouts/shortcodes/blocks/cover.html
layouts/shortcodes/blocks/section.html
layouts/shortcodes/blocks/lead.html
layouts/shortcodes/blocks/link-down.html
```
**目的**: Docsyテーマからの移行互換性  
**影響**: インラインスタイル付きなので、どのテーマでも動作（デザインが統一されない可能性）  
**推奨**: 標準的なMarkdownに置き換えるべき

---

### 4. **カスタム見出しレンダリング**
```
layouts/_markup/render-heading.html
```
**目的**: Docsy特有の見出しレンダリングを置き換え  
**影響**: 見出しにIDが自動付与される  
**推奨**: 必要ならそのまま、不要なら削除

---

### 5. **二重のパーシャルディレクトリ**
```
layouts/partials/     # 空
layouts/_partials/    # 空（非標準的な名前）
```
**問題**: `_partials`は非標準。Hugoは`partials/`を使用  
**推奨**: 両方とも削除（空なので影響なし）

---

### 6. **コンテンツ内の画像配置**
```
content/featured-background.jpg
content/training/about/witness_lee_photo.gif
content/training/history/witness_lee_02.jpg
content/join/short-term/f647907efee133741941e6df4f512c04.webp
```
**方式**: Page Bundle方式（Hugo推奨）  
**影響**: なし（良い方式）  
**推奨**: このまま継続

---

### 7. **重複したロゴ画像**
```
assets/icons/logo.png          # 未使用？
static/images/site-logo.png    # 実際に使用中
```
**問題**: どちらを使うべきか不明確  
**推奨**: `assets/icons/logo.png`を削除

---

### 8. **npm/package.json（Docsy用）**
```
package.json
```
**内容**: Docsy用のビルドスクリプト  
**影響**: PaperModでは不要だが、残しても害はない  
**推奨**: Docsy完全削除なら削除可能

---

### 9. **Docker設定**
```
docker-compose.yaml
Dockerfile
```
**内容**: Hugo開発環境用Docker設定  
**影響**: ローカル開発で使用している場合は必要  
**推奨**: 使用していなければ削除可能

---

## 🎯 推奨される削除リスト

### 即座に削除可能（影響なし）
```bash
# 空のディレクトリ
rm -rf layouts/_default/
rm -rf layouts/_partials/
rm -rf layouts/partials/

# Docsy残骸
rm assets/scss/_variables_project.scss
rm postcss.config.js

# 重複ロゴ（未使用の可能性）
rm assets/icons/logo.png
rmdir assets/icons

# 空になったら削除
rmdir assets/scss
```

### テーマ変更を予定していない場合
```bash
# customテーマ全体
rm -rf themes/custom/
```

### 標準的なMarkdownに置き換えた後
```bash
# カスタムショートコード（コンテンツから削除後）
rm layouts/shortcodes/join-pages.html
rm layouts/shortcodes/training-pages.html
rm layouts/shortcodes/recentposts.html

# Docsy互換ショートコード（コンテンツから削除後）
rm -rf layouts/shortcodes/blocks/
```

---

## ✅ Hugoデフォルト標準構造

この部分は適切です：

```
content/          ✅ 標準
static/           ✅ 標準
themes/           ✅ 標準
hugo.yaml         ✅ 標準（hugo.toml の代わり）
layouts/          ✅ 標準（カスタマイズ用）
.gitignore        ✅ 標準
```

---

## 📊 まとめ

### 現在の状況
- **テーマ**: PaperMod（Git submodule）
- **カスタマイズレベル**: 高（ホームページ完全カスタム）
- **Docsy残骸**: わずかに残存
- **不要ファイル**: 約10-15個

### 推奨アクション
1. ✅ 空のディレクトリとDocsy残骸を削除
2. ✅ 重複ロゴを削除
3. ⚠️ customテーマを削除（PaperMod使用を続ける場合）
4. ⚠️ カスタムショートコードをMarkdownに置き換え
5. ⚠️ layouts/index.htmlの扱いを決定（カスタムorテーマ）

### テーマ独立性
現在の構造では：
- ❌ `layouts/index.html` がテーマ変更を妨げる
- ❌ `layouts/shortcodes/blocks/*` がテーマと競合する可能性
- ✅ コンテンツはテーマ独立
- ✅ 画像配置はテーマ独立

